<?xml version="1.0" encoding="UTF-8" ?>
<odoo>

    <!-- stock_picking action server -->
    <record id="update_kdx_cost_production_action" model="ir.actions.server">
        <field name="name">Establecer costo de producción</field>
        <field name="model_id" ref="stock.model_stock_picking" />
        <field name="binding_model_id" ref="stock.model_stock_picking"/>
        <field name="binding_view_types">form</field>
        <field name="state">code</field>
        <field name="code">action=records.action_update_production_costs()</field>
    </record>

</odoo>